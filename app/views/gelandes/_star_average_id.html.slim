/ 星評価
javascript:
  $("#star-average-#{gelande.id}").raty({
    size: 36,
    starOff: "#{asset_path('star-off.png')}",
    starOn: "#{asset_path('star-on.png')}",
    starHalf: "#{asset_path('star-half.png')}",
    half: true,
    readOnly: true,
    score: "#{gelande.reviews.average(:star).to_f.round(1)}",
  });