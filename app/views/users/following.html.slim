.container
  .row
    .col-lg-4
    .col-lg-4.text-center
      h3.title フォロー

      - @followings.each do |user|
        = attachment_image_tag user, :image, format: 'jpeg', fallback: "noimage.png", size: "60x60", class: ""
        |　
        = link_to user.name, user_path(user)
.mb-5